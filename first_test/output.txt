输入：4096 个候选索引（来自 2 个 8K 块）
输出：前 2048 个最优索引

执行步骤（简化的堆归并）：
1. 初始化一个大小为 2 的最小堆
2. 从每个块的前 2048 个候选中取第一个元素加入堆
3. 重复 2048 次：
   - 弹出堆顶元素（当前最小）
   - 从该元素所在的块中取下一个元素加入堆
   - 将弹出的元素加入结果
4. 返回前 2048 个最大元素的索引

时间复杂度：O(selectedCount × log(numOfBlocks)) ≈ O(2048 × log 2) ≈ 2048 次比较
