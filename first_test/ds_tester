Chat

思考过程

我来搜索代码库中关于 prefill 和 decode 阶段的实现。

在工作区搜索 'prefill|decode'

在工作区搜索 '**/*prefill*'

在工作区搜索 '**/*decode*'

Thought

cann-recipes-infer\models\deepseek-v4

Thought

models\deepseek-v4\models\model_infer.py

Thought

在工作区搜索 'def (prefill|decode)'

Thought

models\deepseek-v4\models\modeling_deepseek.py

Thought

在工作区搜索 'def forward\('

Thought

models\deepseek-v4\models\modeling_deepseek.py

Thought

# DeepSeek V4 Prefill 与 Decode 阶段关键流程分析
## 一、整体架构概览
DeepSeek V4 的推理过程分为两个核心阶段，这两个阶段共享大部分模型结构，但在计算逻辑、缓存管理和并行策略上存在显著差异：

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         DeepSeek V4 推理流程                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────┐    ┌──────────────────┐    ┌──────────────────┐   │
│  │  输入预处理       │───▶│   Prefill 阶段   │───▶│   Decode 阶段    │───▶│  输出  │
│  │  (Tokenize)      │    │  (一次计算全部)   │    │  (逐Token生成)   │   │        │
│  └──────────────────┘    └──────────────────┘    └──────────────────┘   │        
│
│         │                         │                        │            │        
│
│         ▼                         ▼                        ▼            │        
│
│  [B, input_seq_len]      [B, input_seq_len]      [B, 1] → 循环 N 次    │        │
│                         (KV Cache 写入)          (KV Cache 只读)       │        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```
## 二、Prefill 阶段详解
### 2.1 核心目标
Prefill 阶段的核心任务是 一次性处理整个输入序列，计算并填充 KV 缓存 ，为后续的 Decode 阶段做准备。

### 2.2 关键流程
```
┌─────────────────────────────────────────────────────────────────────┐
│                      Prefill 阶段完整流程                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1. 输入嵌入                                                         │
│     input_ids → Embedding → hidden_states [B, S, H]                 │
│                                                                     │
│  2. CP (Context Parallelism) 处理 (可选)                            │
│     - 当 cp_size > 1 时，对输入序列进行分段并行                       │
│     - Zigzag 重排优化内存访问模式                                    │
│                                                                     │
│  3. Rotary Embedding 计算                                            │
│     - 生成多个分辨率的 cos/sin (c1a, comp, c4a, c128a 等)           │
│     - 支持压缩注意力的多尺度位置编码                                  │
│                                                                     │
│  4. 多层 Transformer 前向                                            │
│     for each layer:                                                 │
│       hidden_states = decoder_layer(hidden_states, is_prefill=True)  │
│                                                                     │
│  5. LN + LM Head                                                    │
│     hidden_states → LayerNorm → LM Head → logits [B, S, V]          │
│                                                                     │
│  6. KV Cache 写入                                                    │
│     - 将每一层的 K、V 张量写入 cache_data                            │
│     - 支持量化存储 (W8A8/W4A16)                                     │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```
### 2.3 关键代码分析 2.3.1 Prefill 入口
```
# modeling_deepseek.py:2060-2068
def prefill(
    self,
    **kwargs
):
    logits, prev_hidden_states = self.forward(
        is_prefill=True,  # 关键标志位
        **kwargs
    )
    return logits, prev_hidden_states
```
关键点 ：通过 is_prefill=True 标志位区分执行路径。
 2.3.2 主 Forward 中的 Prefill 处理
```
# modeling_deepseek.py:1526-1611
def forward(
    self,
    input_ids: torch.LongTensor,
    attn_metadata: Optional[Dict] = None,
    cache_data: Optional[Tuple[Dict]] = None,
    is_prefill: Optional[bool] = False,  # 核心控制参数
    cur_topk_list: Optional[torch.Tensor] = None,
):
    batch_size, seq_length = input_ids.shape
    
    # 1. 输入嵌入
    inputs_embeds = self.calc_input_embeddings(input_ids, is_prefill)
    hidden_states = inputs_embeds
    
    # 2. CP (Context Parallelism) 处理
    if is_prefill and self.cp_size > 1:
        # Zigzag 重排优化
        hidden_states_list = list(
            torch.split(hidden_states, attn_metadata["cp_metadata"]["split_list"], 
            dim=1))
        hidden_states = torch.cat(
            [hidden_states_list[i] for i in attn_metadata["cp_metadata"]
            ["zigzag_idx"]], dim=1
        )
    
    # 3. 多分辨率 Rotary Embedding
    if is_prefill and self.cp_size > 1:
        cos_sin = {
            "c1a": self.rotary_emb(hidden_states, position_ids_cur, ...),
            "comp": self.compress_rotary_emb(hidden_states, position_ids_cur, ...),
            "c4a": self.compress_rotary_emb(hidden_states, position_ids_cmp
            ["4"], ...),
            "c128a": self.compress_rotary_emb(hidden_states, position_ids_cmp
            ["128"], ...),
            # ... 其他分辨率
        }
    
    # 4. MHC (Multi-Head Context) 扩展
    hidden_states = hidden_states.unsqueeze(2).repeat(1, 1, self.hc_mult, 1)
    
    # 5. 逐层前向（关键循环）
    with superkernel_scope(self.enable_superkernel and not is_prefill, label, 
    option):
        for decoder_layer in self.layers:
            hidden_states = decoder_layer(
                hidden_states,
                attn_metadata=attn_metadata,
                past_residual=residual,
                cache_data=cache_data,
                is_prefill=is_prefill,  # 传递给每层
                cur_topk_list=cur_topk_list,
                input_ids=input_ids_cp,
            )
    
    # 6. 输出处理
    hidden_states = self.hc_head(hidden_states, self.hc_head_fn, self.
    hc_head_scale, self.hc_head_base)
    hidden_states = self.norm(hidden_states)
    return hidden_states
```
核心差异点 ：

- is_prefill=True 时， superkernel_scope 被禁用（第1598行）
- CP 处理和多分辨率 RoPE 只在 prefill 阶段执行 2.3.3 Mini-Batch Prefill 机制
```
# model_infer.py:637-662
if self.main_model.prefill_mini_batch_size > 0:
    self.prefill_cycles = batch_size // self.main_model.prefill_mini_batch_size
else:
    self.prefill_cycles = 1

# Prefill 循环
for cycle_idx in range(self.prefill_cycles):
    # 切片处理单个 mini-batch
    input_dict_main_single_cycle = self.process_mini_batch_inputs(input_dict_main, 
    cycle_idx)
    
    # 执行推理
    input_dict_main_single_cycle, \
    input_dict_mtp_single_cycle, \
    mtp_argdict_single_cycle = self.prefill_infer_single_cycle(...)
```
设计目的 ：

- 当 batch_size 较大时，将 prefill 拆分为多个 mini-batch 执行
- 减少单次内存占用，避免 OOM
- 与 TP/EP 并行策略配合，实现更细粒度的负载均衡
## 三、Decode 阶段详解
### 3.1 核心目标
Decode 阶段的核心任务是 基于 Prefill 阶段构建的 KV 缓存，逐 Token 生成输出序列 。

### 3.2 关键流程
```
┌─────────────────────────────────────────────────────────────────────┐
│                      Decode 阶段完整流程                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  while cnt < max_new_tokens:                                        │
│    ┌──────────────────────────────────────────────────────────────┐ │
│    │ 1. 输入嵌入 (仅当前token)                                     │ │
│    │    input_ids [B, 1] → Embedding → hidden_states [B, 1, H]    │ │
│    └──────────────────────────────────────────────────────────────┘ │
│                              │                                      │
│                              ▼                                      │
│    ┌──────────────────────────────────────────────────────────────┐ │
│    │ 2. 单层 Rotary Embedding                                     │ │
│    │    - 仅计算当前位置的 cos/sin                                 │ │
│    │    - 复用 prefill 阶段的缓存                                  │ │
│    └──────────────────────────────────────────────────────────────┘ │
│                              │                                      │
│                              ▼                                      │
│    ┌──────────────────────────────────────────────────────────────┐ │
│    │ 3. 多层 Transformer 前向 (重用 KV Cache)                       │ │
│    │    for each layer:                                           │ │
│    │      hidden_states = decoder_layer(                           │ │
│    │          hidden_states,                                      │ │
│    │          is_prefill=False,  # 关键区别                        │ │
│    │          cache_data=cache_data  # 只读模式                     │ │
│    │      )                                                       │ │
│    └──────────────────────────────────────────────────────────────┘ │
│                              │                                      │
│                              ▼                                      │
│    ┌──────────────────────────────────────────────────────────────┐ │
│    │ 4. LM Head + Sampling                                        │ │
│    │    logits [B, 1, V] → sample → next_token [B, 1]             │ │
│    └──────────────────────────────────────────────────────────────┘ │
│                              │                                      │
│                              ▼                                      │
│    ┌──────────────────────────────────────────────────────────────┐ │
│    │ 5. 更新状态                                                   │ │
│    │    - input_ids = next_token                                  │ │
│    │    - kv_len += 1                                             │ │
│    │    - generate_ids 追加                                        │ │
│    └──────────────────────────────────────────────────────────────┘ │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```
### 3.3 关键代码分析 3.3.1 Decode 入口
```
# modeling_deepseek.py:2090-2098
def decode(
    self,
    **kwargs
):
    if self.enable_cache_compile:
        logits = self.cached_decode(**kwargs)  # 缓存编译优化
    else:
        logits = self.main_decode(**kwargs)    # 普通解码
    return logits
```
缓存编译优化 ：当 enable_cache_compile=True 时，使用 cached_decode 避免重复编译
 3.3.2 Main Decode 实现
```
# modeling_deepseek.py:2070-2078
def main_decode(
    self,
    **kwargs
):
    logits, prev_hidden_states = self.forward(
        is_prefill=False,  # 关键标志位
        **kwargs
    )
    return logits, prev_hidden_states
```
注意 ：decode 同样调用 self.forward() ，但 is_prefill=False
 3.3.3 Decode 循环控制
```
# model_infer.py:710-728
with decode_profiler as prof:
    while True:
        # 退出条件检查
        jump_flag = self.get_jump_flag(decode_cnt, warm_up)
        if jump_flag:
            break
        
        if self.mtp_model is None:
            # 普通解码
            _, infer_time_main = self.model_inference(input_dict_main, 
            warm_up=warm_up)
            decode_infer_time_rec.append(infer_time_main)
        else:
            # MTP (Multi-Token Prediction) 加速解码
            input_dict_main, \
            input_dict_mtp, \
            infer_time_main, \
            infer_time_mtp = self.model_inference_mtp(input_dict_main, 
            input_dict_mtp,
                                                      mtp_argdict, warm_up=warm_up)
            decode_infer_time_rec.append(infer_time_main + sum(infer_time_mtp))

        prof.step()
        generate_tokens += 1
        decode_cnt += 1
``` 3.3.4 MTP 加速机制
```
# model_infer.py:194-222
def model_inference_mtp(self, input_dict_main, input_dict_mtp=None,
                        mtp_argdict=None, cycle_idx=None, warm_up=False):
    # 1. 主模型推理（生成 1 个 token）
    model_inputs = self.model_input_prepare(self.main_model.model, input_dict_main)
    outputs, prev_hidden_states, infer_time_main = self.inference(self.main_model, 
    model_inputs,
                                                                  is_prefill=input_
                                                                  dict_main
                                                                  ['is_prefill'],
                                                                  cycle_idx=cycle_i
                                                                  dx,
                                                                  warm_up=warm_up, 
                                                                  prefix='
                                                                  [MainModel]')
    
    # 2. 采样主模型输出
    mtp_argdict['main_next_tokens'] = self.sample(outputs, temperature=self.
    temperature)
    
    # 3. 验证 MTP 预测的 tokens
    mtp_argdict['accepted_num'] = self.verify_spec_tokens(
        input_dict_main, input_dict_mtp, mtp_argdict['main_next_tokens'])
    
    # 4. MTP 模型推理（预测 next_n 个 tokens）
    input_dict_mtp, infer_time_mtp = self.mtp_post_process(input_dict_mtp, 
    cycle_idx, warm_up)
    
    # 5. 更新主模型输入（追加 MTP 预测的 tokens）
    mtp_spec_tokens = input_dict_mtp['spec_tokens']
    input_dict_main['input_ids'] = torch.cat([input_dict_main['input_ids'], 
    mtp_spec_tokens], dim=1)
    
    return input_dict_main, input_dict_mtp, infer_time_main, infer_time_mtp
```
MTP 工作原理 ：

1. 主模型生成 1 个 token
2. MTP 模型（较小的 draft 模型）预测 next_n 个 token
3. 主模型验证这些预测，接受正确的部分
4. 跳过已验证的 token 计算，直接进入下一轮
## 四、Prefill vs Decode 核心差异对比
维度 Prefill 阶段 Decode 阶段 输入序列长度 完整输入序列 [B, S] 单 Token [B, 1] KV Cache 模式 写入模式（填充缓存） 只读模式（重用缓存） 计算复杂度 O(S²)（全注意力） O(S)（稀疏/滑动窗口注意力） 内存访问模式 写密集型 读密集型 SuperKernel 禁用 启用（优化 decode 性能） CP 并行 启用（如果配置） 禁用 MTP 加速 不适用 启用（如果配置） RoPE 计算 多分辨率（c1a, comp, c4a, c128a） 单分辨率 推理次数 1 次（或 mini-batch 多次） max_new_tokens 次 时间占比 通常 < 10% 通常 > 90%

## 五、关键优化技术
### 5.1 SuperKernel 优化
```
# modeling_deepseek.py:1598
with superkernel_scope(self.enable_superkernel and not is_prefill, label, option):
    for decoder_layer in self.layers:
        hidden_states = decoder_layer(...)
```
设计原理 ：

- Decode 阶段执行次数远多于 Prefill
- SuperKernel 将多个算子融合为单个二进制 kernel
- 减少 kernel launch 开销和中间结果内存访问
### 5.2 Cache Compile 优化
```
# modeling_deepseek.py:2094-2097
if self.enable_cache_compile:
    logits = self.cached_decode(**kwargs)  # 复用编译结果
else:
    logits = self.main_decode(**kwargs)
```
设计原理 ：

- 首次 decode 时编译计算图
- 后续迭代直接复用编译结果
- 跳过 guard check，减少延迟
### 5.3 多流并行
```
# model_infer.py:1590-1593
if self.enable_multi_streams:
    option = "stream-fusion=1"  # 启用多流融合
else:
    option = "option_xxx2"
```
设计原理 ：

- 将计算和通信/内存操作分配到不同 stream
- 实现计算-通信重叠
- 提升整体吞吐量
## 六、总结
### 6.1 Prefill 阶段特点
1. 一次性计算 ：处理完整输入序列
2. 缓存构建 ：写入 KV 缓存供后续使用
3. 高计算复杂度 ：O(S²) 注意力计算
4. 并行策略丰富 ：支持 TP/EP/DP/CP 多种并行组合
### 6.2 Decode 阶段特点
1. 迭代计算 ：逐 token 生成
2. 缓存重用 ：只读访问 KV 缓存
3. 低计算复杂度 ：O(S) 或 O(K)（稀疏注意力）
4. 优化技术密集 ：SuperKernel、MTP、Cache Compile 等
### 6.3 性能优化重点
- Prefill 优化 ：减少内存占用（mini-batch）、优化并行策略
- Decode 优化 ：减少单次迭代延迟（SuperKernel、MTP）、提升吞吐量（多流）
这两个阶段的设计体现了 LLM 推理的核心优化思想： 通过预计算和缓存复用，将大部分计算开销转移到单次的 Prefill 阶段，使高频的 Decode 阶段尽可能高效 。

15:08
华为

为什么deepseek v4在推理时prefiil阶段的LightningIndexer算子耗时会越来越大

Chat

思考过程

我来分析 DeepSeek V4 中 LightningIndexer 算子在 Prefill 阶段耗时变化的原因。

ops\pypto\src\lightning_indexer_pto\op_kernel\lightning_indexer_topk.cpp

Thought

在工作区搜索 'casualOffset|effSeq|actBlock'

Thought

# DeepSeek V4 Prefill 阶段 LightningIndexer 耗时递增问题分析
## 一、问题现象
在 DeepSeek V4 的 Prefill 阶段，随着输入序列位置 s1Idx 的递增， LightningIndexer 算子的耗时会 逐渐增大 。这是一个由算子设计本身导致的 预期行为 ，而非 bug。

## 二、核心原因：有效序列长度递增
### 2.1 关键代码分析
```
// lightning_indexer_topk.cpp:75-79
LOOP("INDEX_LOOP_S1", FunctionType::DYNAMIC_LOOP, s1Idx, LoopRange(s1)) {
    // casual inference
    auto casualOffset = s1 - s1Idx - 1;
    auto effSeq = curSeq - casualOffset;
    auto actBlock = (effSeq + blockSize - 1) / blockSize;
```
变量解读 ：

变量 含义 示例（s1=512, curSeq=512） s1 输入序列总长度 512 s1Idx 当前处理的序列位置（0~s1-1） 0, 1, 2, ..., 511 casualOffset 因果偏移量 511, 510, 509, ..., 0 effSeq 有效序列长度（当前位置能看到的 Key 数量） 1, 2, 3, ..., 512 actBlock 有效块数 1/128, 2/128, ..., 4

### 2.2 数学推导
根据代码逻辑：

```
casualOffset = s1 - s1Idx - 1
effSeq = curSeq - casualOffset

当 curSeq == s1 时（Prefill 阶段）：
effSeq = s1 - (s1 - s1Idx - 1) = s1Idx + 1
```
结论 ：在 Prefill 阶段，有效序列长度 effSeq 线性递增 ：

- 第 0 个位置：只能看到自己， effSeq = 1
- 第 1 个位置：能看到前 2 个位置， effSeq = 2
- ...
- 第 s1-1 个位置：能看到全部 s1 个位置， effSeq = s1
## 三、耗时递增的机制
### 3.1 分级 TopK 算法路径
LightningIndexer 针对不同 effSeq 长度采用 不同的 TopK 算法 ：

```
// lightning_indexer_topk.cpp:153-154
auto lengthIsLE2K = effSeq <= length2K;    // 2048
auto lengthIsGT2K = effSeq > length2K;
auto lengthIsLE8K = effSeq <= length8K;    // 8192
auto lengthIsGT8K = effSeq > length8K;     // >8192
```
序列长度范围 算法策略 时间复杂度 相对耗时 ≤ 2048 直接排序 O(n log n) 1x (2048, 8192] 直接排序 O(n log n) ~4x > 8192 两级归并 O(n log K) ~8-16x

### 3.2 计算量分析
以 64K 序列为例，不同位置的计算量对比：

位置 effSeq 算法路径 块数（blockSize=128） 相对计算量 0 1 直接排序 1 1x 1024 1025 直接排序 9 ~10x 2048 2049 直接排序 17 ~17x 8192 8193 直接排序 65 ~65x 16384 16385 两级归并 129 ~129x 65536 65536 两级归并 512 ~512x

### 3.3 两级归并的额外开销
当 effSeq > 8192 时，采用两级归并策略，引入额外开销：

```
// lightning_indexer_topk.cpp:257-260
LOOP("128K_TO_32K_FULL_SORT", FunctionType::DYNAMIC_LOOP, idx1, LoopRange
(numOf8KFullBlock * lengthIsGT8K)) {
    auto effSumRes = View(localSum, {1, maxS2}, {1, effSeq}, {bs1n2Offset, 0});
    auto ax = View(effSumRes, {1, length8K}, {0, idx1 * length8K});
    auto [res, tmp] = TopKSort(ax, idx1);
```
两级归并流程 ：

1. 第一级 ：将 128K 分为 16 个 8K 块，每块选 Top-2048，得到 32K 候选
2. 第二级 ：将 32K 分为 4 个 8K 块，每块选 Top-2048，得到 8K 候选
3. 第三级 ：8K → Top-2048 最终结果
## 四、Prefill 阶段耗时分布
### 4.1 理论耗时曲线
```
┌─────────────────────────────────────────────────────────────────┐
│                     Prefill 阶段耗时分布                         │
├─────────────────────────────────────────────────────────────────┤
│  耗时 (ms)                                                      │
│      ^                                                          │
│      │                                                          │
│   500│                          *                               │
│      │                     *    *                               │
│   400│              *    *    *    *                            │
│      │         *    *    *    *    *                            │
│   300│    *    *    *    *    *    *                            │
│      │ *    *    *    *    *    *    *                          │
│   200│*    *    *    *    *    *    *    *                      │
│      │                                                          │
│   100│                                                          │
│      │                                                          │
│    0└───────────────────────────────────────────────────────────▶
│        0   16K    32K    48K    64K    80K    96K   128K        │
│                   序列位置 (s1Idx)                              │
│                                                                 │
│  说明: 曲线呈抛物线增长，最后一个位置(s1Idx=s1-1)耗时最大         │
└─────────────────────────────────────────────────────────────────┘
```
### 4.2 各阶段耗时占比
以 64K 序列为例（假设每个位置平均耗时）：

阶段 位置范围 占比 快速增长期 0 ~ 2048 ~5% 平稳增长期 2048 ~ 8192 ~15% 缓慢增长期 8192 ~ 65536 ~80%

## 五、代码优化建议
当前实现中， casualOffset 的计算方式存在逻辑不清晰的问题。建议修改为更直观的形式：

```
// 优化前（当前实现）
auto casualOffset = s1 - s1Idx - 1;
auto effSeq = curSeq - casualOffset;

// 优化后（更清晰的因果约束表达）
auto effSeq = s1Idx + 1;  // 第 s1Idx 个位置能看到前 s1Idx+1 个位置
```
优化收益 ：

1. 可读性提升 ：直接表达因果约束的语义
2. 潜在优化空间 ：可考虑对前 N 个位置采用更轻量的计算策略
3. 便于调试 ： effSeq 的含义一目了然
## 六、总结
### 根本原因
LightningIndexer 在 Prefill 阶段耗时递增是由因果注意力机制决定的预期行为 ：

1. 序列中的第 i 个位置只能关注前面的 i+1 个位置（因果约束）
2. 随着位置后移，有效序列长度 effSeq 线性递增
3. TopK 算法的时间复杂度为 O(n log n)，计算量随序列长度呈超线性增长
4. 当 effSeq > 8192 时，算法切换为两级归并，引入额外的归并开销
### 性能影响
对于长序列（如 128K）， 最后一个位置的计算量可能是第一个位置的 1000 倍以上 。这也是为什么 Prefill 阶段的总耗时会随输入序列长度急剧增加的原因。

### 缓解策略
1. 限制输入序列长度 ：对超长输入进行截断
2. 启用 Mini-Batch Prefill ：将大 batch 拆分为多个小 batch
3. 优化 TopK 算法 ：针对不同长度范围采用更高效的实现
